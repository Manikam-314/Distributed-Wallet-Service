apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: distributed-wallet
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: distributed-wallet/api-gateway:latest
        ports:
        - containerPort: 8090
        envFrom:
        - configMapRef:
            name: wallet-config
        resources:
          limits:
            cpu: "400m"
            memory: "512Mi"
          requests:
            cpu: "200m"
            memory: "256Mi"
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8090
          initialDelaySeconds: 25
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8090
          initialDelaySeconds: 40
          periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: distributed-wallet
spec:
  selector:
    app: api-gateway
  ports:
  - protocol: TCP
    port: 8090
    targetPort: 8090
  type: LoadBalancer